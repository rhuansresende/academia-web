import { Environment } from "../app/model/Environment";

export const environment: Environment = {
  production: true,
  appName: 'ACADEMIA-WEB',
  envName: 'PROD',
  test: false,
  BASE_URL: '/api'
}
